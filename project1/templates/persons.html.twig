<paper-dropdown-menu label="Persons" id="person">
    <paper-listbox slot="dropdown-content" selected="1">
        {% for user in users %}
            <paper-item>{{ user.username }}</paper-item>
        {% endfor %}
    </paper-listbox>
</paper-dropdown-menu>
